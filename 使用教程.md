# rPPG 人脸心率 + 血压 —— 零基础上手教程

本项目逆向自 `offline_engine-release.zip`（cmcr offline engine）的 rPPG 算法层，
实现：调用摄像头 → 人脸 RGB → PPG 波形 → 心率 + **真实血压(mmHg)**。

> 血压返回的是真实收缩压/舒张压(mmHg)，不是风险分数。
> 完整逆向结论见 `README.md` 的"血压模型说明"。

---

## 一、把项目拿下来

```bash
git clone https://github.com/lhx20011226/rppg_temp.git
cd rppg_temp
```

---

## 二、准备 Python 环境（三种系统）

本项目需要 **Python 3.11**。先确认本机有 python3：

```bash
python3 --version
```

没有就去 https://www.python.org/downloads/ 装 3.11.x。

### 1) 建虚拟环境

Linux / macOS：
```bash
python3 -m venv rppg_venv
source rppg_venv/bin/activate
```

Windows（PowerShell）：
```powershell
python3 -m venv rppg_venv
.\rppg_venv\Scripts\Activate.ps1
```
> 若 Windows 报"禁止运行脚本"，先执行一次：
> `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`（选 Y），再重跑上面的激活命令。

激活成功后命令行前面会出现 `(rppg_venv)`，看到它就对了。

### 2) 装依赖

```bash
(rppg_venv) pip install -r requirements.txt
```

装完 `bp_models_full/` 目录要跟 `rppg_cam.py` 同级（已经在仓库里了，不用动）。

---

## 三、跑代码

### 方案 A：没有摄像头也能跑（先验证整条链路）

```bash
(rppg_venv) python repro_rppg_bp.py
```

预期输出（数值示例如下，合成数据仅供验证链路）：
```
[心率] 估计 HR ≈ 75.0 bpm
[窗口特征] flag=0, n_valid_cycles=60, 特征维度=74
[组特征] flag=0, 维度=74
[血压] 收缩压=149.0 mmHg  舒张压=80.2 mmHg  (融合原始值 hbp_raw=149.0, lbp_raw=80.2)
```
最后一行就是 **真实收缩压 / 舒张压 (mmHg)**。

### 方案 B：用摄像头实时测（需要摄像头 + 桌面环境）

```bash
(rppg_venv) python rppg_cam.py --seconds 30 --height 170 --age 30 --gender 1
```

参数按你本人改：
- `--seconds 30` 采集时长（建议 30~60，越长越准）
- `--height 170`  身高 cm
- `--age 30`      年龄
- `--gender 1`    1=男 0=女
- `--camera 0`    摄像头编号（默认 0；多摄像头试 1、2）

坐稳别动，结束输出心率和血压。

---

## 四、退出环境

```bash
(rppg_venv) deactivate
```

---

## 五、常见问题

**`python: command not found`** → 用 `python3`。
**`rppg_cam.py` 提示"摄像头采集失败"** → 当前机器没摄像头，属正常，用方案 A 验证。
**lightgbm 报错 "Estimator not fitted"** → 必须 `lightgbm==3.3.2`，重装：
```bash
(rppg_venv) pip install --no-deps lightgbm==3.3.2
```
**模型加载报 `n_features_` 错** → 确认 `scikit-learn==1.1.3` 与 `numpy==1.23.5` 版本无误。

---

## 六、项目结构

```
rppg_temp/
├── rppg_venv/          # 你本地建的虚拟环境（不进 git）
├── bp_models_full/     # 血压模型 (joblib .pkl + features.txt)
├── rppg_cam.py         # 摄像头主程序 (心率+血压)
├── repro_rppg_bp.py    # 离线 demo (合成 PPG 跑通整条链路)
├── bp_inference.py     # 血压推理封装 (加载回归模型 + 融合 + 基线夹逼)
├── features_74.py      # 74 维 PPG 特征定义
├── requirements.txt    # 依赖锁定版本
├── README.md           # 逆向结论 + 调用链
└── 使用教程.md         # 本文件
```
